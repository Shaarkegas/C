#include <stdio.h>
#include <stdlib.h>

int graus()
{
    int r = 2;
    float pi = 3.14;
    float A = pi * (r * r);
    printf("A area do circulo de raio %i eh igual a: %.2f",r,A);
    return 0;
}
